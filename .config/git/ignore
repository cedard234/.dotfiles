# ── macOS ──
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd
.DocumentRevisions-V100
.TemporaryItems
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# ── Linux ──
*~
.directory
.Trash-*
.nfs*

# ── VSCode ──
# Note: many teams DO commit .vscode/ for shared settings.
# Only ignore globally if you prefer per-project opt-in.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# ── JetBrains (CLion/IDEA/etc.) ──
.idea/
*.iml
*.iws
cmake-build-*/

# ── Vim ──
*.swp
*.swo
[._]*.un~
Session.vim

# ── Editors / misc temp ──
*.bak
.*.tmp

# ── Tags / ctags ──
tags
.tags

# ── Direnv / env ──
.envrc
.direnv/

# ── Secrets you never want to leak by accident ──
*.pem
.env.local
